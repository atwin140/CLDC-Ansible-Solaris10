- name: V-24347 Fail
  blockinfile:
    dest: "{{out_file}}"
    state: present
    marker: " "
    block: |
      V-24347
      The system, if capable, must be configured to require the use of a CAC, PIV compliant hardware token, or Alternate Logon Token (ALT) for authentication.
      
      Solaris 10 does not support CAC Auth.  RSA-token is currently being implmented

      ====================================================================
      This is a FINDING
      -
