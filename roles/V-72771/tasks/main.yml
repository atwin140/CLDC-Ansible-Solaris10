- name: V-72771 Pass
  blockinfile:
    dest: "{{out_file}}"
    state: present
    marker: "----------------------------------------------------------------------------------------------"
    block: |
      V-72771
      Wireless network adapters must be disabled.
      This is N/A for systems that do not have wireless network adapters.
      Procedure:
      #  These server do not have have wireless adaptors.
      -
      This is Not Applicable
      -
